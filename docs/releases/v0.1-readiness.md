# Pactmark v0.1 readiness record

Status: **public source candidate; not ready for package release**  
Profile: `release` metadata; publication not authorized  
Publication: `not_authorized`  
Record time: `2026-08-04T11:46:25Z`

This record separates reproducible local evidence and the authorized public source push from work that still needs external infrastructure, credentials, legal/owner decisions, or a separate publication write. It is not a security certification, deployment attestation, availability promise, npm-release claim, or statement that Pactmark is production-ready.

## Candidate identity

The strongest local candidate evidence is the isolated `pnpm verify:clean-source` run completed at `2026-08-04T08:05:02.548Z` after the real GitHub repository metadata was packed across all 19 packages:

| Field                         | Value                                                                     |
| ----------------------------- | ------------------------------------------------------------------------- |
| Public source files           | 733                                                                       |
| Canonical source-copy digest  | `sha256:8b3de2d201501e39dc463eb5c16fcbcc86151573aff61391030cd819556134e8` |
| Synthetic baseline commit     | `88c351f960c7aeab50a5cbdca3f2550e98e7358f`                                |
| Synthetic baseline Git tree   | `0d893b3fa3f5438e006fdbe864e218ef204bca35`                                |
| Release source-tree digest    | `sha256:23840bdb63bf0fb30fe3a8799d732b15bb75d00ea61a4c602910d482e249bb89` |
| Clean release manifest digest | `sha256:dfd15b7caefbaae5211a9fb9b2ac1fd4cbfcc7106158f10b039b545261e47ee2` |
| Bootstrap log digest          | `sha256:f28e8aab10d6cfb79283e3faecc0f7144585fb2b409da444b72fe72fb4bfb538` |
| Full verification log digest  | `sha256:d947656bf3c9a10a051c9e7311a9a4bb5a575295eeaaca2d83ace890a8338c3d` |
| Consumer lock-template digest | `sha256:c14845d61517a71ba29cb1666a8e1440a5d9bec7ef88b44c0ed734e1fed75070` |
| Post-verification state       | `clean: true`; 19 packages                                                |

The synthetic commit exists only inside the disposable conformance checkout. It proves that the exact public export bootstraps and verifies from a clean source identity; it is not the maintainer commit, tag, signature, attestation, or frozen package-release candidate. The PLAN/readiness evidence-record edits that describe the completed run follow that snapshot and receive targeted format/docs checks rather than retroactively changing its identity. After that proof, explicit authorization published root maintainer commit `3234ae5e0d5e7855d67aa3010cd2a12f88e86d3d` to `main` at exact origin `https://github.com/pactmark/pactmark.git`. The pre-commit local release manifest remains an honest snapshot with `clean: false`, `commit: uncommitted-local-source`, and `publication: not_authorized`; it is not relabeled after the fact.

The public source repository and default `main` branch now exist under inspected authority. Public implementation commit `8f18f563645ed784b3d497941afedbd6cf5527f5` passed the full GitHub Actions host/runtime matrix in run [`30901949762`](https://github.com/pactmark/pactmark/actions/runs/30901949762). PR [#1](https://github.com/pactmark/pactmark/pull/1) then activated CODEOWNERS and made the standalone deterministic-security workflow reproduce its clean-run bootstrap/build prerequisites; runs [`30905174400`](https://github.com/pactmark/pactmark/actions/runs/30905174400) and [`30905174388`](https://github.com/pactmark/pactmark/actions/runs/30905174388) passed before merge commit `904a5c90796a824591e0f24017bff9223ef3f824`. No release tag, GitHub Release, npm publication, trusted publisher, signature, attestation, or live deployment is represented by these values.

## Toolchain and environment

| Component                                   | Exact value              |
| ------------------------------------------- | ------------------------ |
| Host                                        | `darwin arm64`           |
| Node.js used for canonical local run        | `v22.22.3`               |
| Supported release lines in package metadata | Node 22.14+ or Node 24.x |
| pnpm                                        | `11.18.0`                |
| TypeScript                                  | `6.0.3`                  |
| Vitest                                      | `4.1.10`                 |
| Turbo                                       | `2.10.8`                 |
| tsx                                         | `4.23.5`                 |
| ESLint                                      | `10.8.0`                 |
| Prettier                                    | `3.9.6`                  |
| API Extractor                               | `7.58.12`                |
| publint                                     | `0.3.22`                 |
| Are the Types Wrong                         | `0.18.5`                 |
| size-limit                                  | `13.0.3`                 |

The lockfile is the dependency-version authority. Its digest is `sha256:9269d84c439fbc3c0a942fba8d14cfcfbfe15af0ac234345c145e17530b751a5`.

## Canonical verification result

The exact aggregate command was:

```sh
pnpm verify
```

At `2026-08-04T08:05:02.548Z` it exited zero inside the clean export on Node `v22.22.3`/macOS arm64. It executed release-profile naming/repository consistency, manifest consistency, formatting, lint, strict typecheck, build, unit/integration/consumer/portability/example suites, documented commands, memory/Postgres contracts, migrations, two-process durable resume, PostgreSQL TLS, OCI conformance, Vercel/UI contracts, Cloudflare dry-run, performance, API/registration/dependency boundaries, unused/placeholder checks, adversarial security, offline advisory/license/workflow/secret/release audits, SBOM, the 93-page documentation build and checks, deterministic pack checks, and release dry-run.

The complete isolated clean-source log is retained with the digest above. Current live advisory and external-link gates passed separately. Public run [`30901949762`](https://github.com/pactmark/pactmark/actions/runs/30901949762) subsequently passed the canonical `pnpm verify` on Ubuntu Node 22.23.2 and 24.18.1 and `pnpm verify:portable-host` on Node 24.18.1 macOS and Windows. Authenticated post-merge inspection also proved the repository protection/reporting controls described below. WP-11 remains open for manual screen-reader/keyboard/true-zoom checks, authorized live preview, frozen-candidate freshness, and package-release work—not for lack of a clean local source export, maintainer commit, packed consumer, exact repository metadata, documentation, required host/runtime CI proof, or GitHub repository protection.

## Package snapshot

All 19 packages expose the documented ESM entry with declarations. `@pactmark/cli` owns the `pactmark` binary and `create-pactmark` owns the initializer binary. The local dry run rejected private inputs, environment files, tests, workspace links, coverage, and caches from tarballs.

| Package                            | Candidate tarball SHA-256                                          |
| ---------------------------------- | ------------------------------------------------------------------ |
| `@pactmark/agent`                  | `3f2efc2290b947edaf67bd8dd356fa305c47ff6f131a16cbd459dc3417104e59` |
| `@pactmark/ai-sdk`                 | `4b1bac5c38cc4a8f9131169a09ffa6d8550fe7e65209ed2d5c723f7531b41faa` |
| `@pactmark/cli`                    | `8a472a49a83f8aaa60bb6607aeced0b2f6a0a786be2a7ee564bf0b71e200e0bf` |
| `@pactmark/cloudflare`             | `0cd0474b376dbbdd196fa50a01378914c67872d4e0e6dfe7c5ed5cd1164267f1` |
| `@pactmark/core`                   | `2d885c87760f0ce2cf27be09e1bd4993bafb116719cfc3c71a1749e09f8f4553` |
| `@pactmark/driver-postgres-worker` | `39758b9714f44df884b5282cca5363909a9654db411fdac368795f27284ce41f` |
| `@pactmark/evidence`               | `ce1d11d439e54dc51cfcae4dee2f961bb8a521e7592261332f943f146c9b0454` |
| `@pactmark/executor-in-process`    | `619fc2be01c612b5defcddca9eece323f779479e356d3f12c7ac7f131eee406c` |
| `@pactmark/http`                   | `01dfadeb7f94a7d35e8ccd9d7925fca3a141d34755e11ca8c627b95399fe2084` |
| `@pactmark/mcp`                    | `d2ecc3ab0894b6e30e63ba5cfa924e3dcab65027fb1fb578cce1bd4ddf940113` |
| `@pactmark/node`                   | `9bd24f1651d5e003b82f9a5467507a32d2dc031dbec10aca48e3681e94b9658d` |
| `@pactmark/otel`                   | `ab7c5a99165d4255ef971b634e859fcaeab9ab6c39bf2e6b6e331b2c1da2f5be` |
| `@pactmark/policy`                 | `c749fa88739c4ff6ce4ce549ef032f30fa61ba3c8b038a918e6c100ea42d54d2` |
| `@pactmark/runtime`                | `84e7b310b37535dc9ac88ab307e9f46502ff0864b053abcde84ed1a472f894bb` |
| `@pactmark/store-memory`           | `94c16eba02eef1636b710bb822586570ad2e2e2758f52bef56acec68da877eae` |
| `@pactmark/store-postgres`         | `b8f76d47d490f3301d6b8e88e3d637bc22a301535906088c6bab2cbe37213352` |
| `@pactmark/testing`                | `cd177fdf266486bb33e7c5ac93cfc3cf6c5bbad7f4f509cd96f7c673e644e7f6` |
| `@pactmark/vercel`                 | `10976cd8fb72757f2076af5fa490e2907fd4e991040488420c21e56b0e57dfa8` |
| `create-pactmark`                  | `f62bf497144e4c139d480689e0d083b8163f2a7b614f9357f5387d1e24756097` |

Independent consumers passed NodeNext and Bundler resolution from absolute tarballs. Every candidate was produced twice from canonical staging and required identical npm metadata and SHA-256 before acceptance. The loopback registry published exact candidates dependency-first and initializer-last, then ran the npm initializer without changing global npm configuration. Independent Yarn 4 and Bun consumers installed/imported the packed core candidate.

## Local compatibility matrix

| Dimension                                     | Status             | Evidence boundary                                                                                                                                                        |
| --------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Node.js 22 on macOS arm64                     | `build-compatible` | Canonical local build, tests, consumers, benchmarks, PostgreSQL, and Docker orchestration used Node `v22.22.3`.                                                          |
| Node.js 22.23.2 on Linux arm64                | `build-compatible` | Digest-pinned Bookworm image passed the offline portable-host gate: 43 typechecks, 30 builds, 43 unit tasks, 8 consumers, and 19 portability tasks, with zero downloads. |
| Node.js 24.18.1 on Linux arm64                | `build-compatible` | Digest-pinned Bookworm image passed the same offline portable-host gate and counts with zero package downloads.                                                          |
| Node.js 24.18.1 on macOS/Windows              | `build-compatible` | Public run `30901949762` passed typecheck, build, unit, independent packed-consumer, and portability gates on macOS 14 and Windows.                                      |
| Node server                                   | `build-compatible` | Loopback HTTP/SSE and quickstart contracts passed locally.                                                                                                               |
| Node OCI image                                | `build-compatible` | Offline host materialization, build network `none`, read-only runtime container, health/run/inspection, and cleanup passed locally. No registry or multi-arch claim.     |
| Next.js/Vercel Node adapter                   | `build-compatible` | Local build, routes, security, accessibility, and adapter contracts passed. No Vercel URL/log was inspected.                                                             |
| Vercel preview/production                     | `unsupported`      | No live deployment was authorized or attempted.                                                                                                                          |
| Cloudflare Worker                             | `experimental`     | Portable tests, generated types, and Wrangler dry-run passed; no staging deployment.                                                                                     |
| Memory persistence                            | `build-compatible` | Deterministic and tenant-scoped but explicitly ephemeral; not production durability.                                                                                     |
| PostgreSQL durable profile                    | `build-compatible` | PostgreSQL 17 migrations, 64 unit, 11 real integration, one worker, TLS, concurrency, clock-skew, and two-process resume gates passed locally. Not production-verified.  |
| npm initializer/local registry                | `build-compatible` | Exact candidate publication and initializer execution passed against an ephemeral loopback registry.                                                                     |
| Yarn 4/Bun packed library                     | `build-compatible` | Independent packed-library install/import passed locally.                                                                                                                |
| Direct runtime, HTTP JSON/SSE, MCP stdio/HTTP | `build-compatible` | Deterministic local contract evidence; no live provider/platform claim.                                                                                                  |

The final OCI conformance run built with network disabled and produced local image digest `sha256:f8457be58ddda12745c93eeea0aca6211d75eb65491933fa5fe86672e4001dba`. The read-only container returned health 200, honestly returned readiness 503 for its ephemeral profile, completed a streamed run, persisted terminal inspection, failed terminal cancellation closed with `KAF_SCHEMA_INVALID`, exposed no secret-named environment value, and was removed.

## Durability and effect evidence

The canonical disposable PostgreSQL 17 gate applied migrations through 010 and passed the real database/worker/TLS suites. Its two-process scenario:

1. started a run over authenticated HTTP in process A;
2. used the sealed registered model path with a WorkOrder-postdated credential and durable model-call reservation;
3. dispatched and acknowledged a native governed effect;
4. killed process A with `SIGKILL`;
5. created a fresh process B/runtime/store/protector;
6. reloaded the protected result and completed/verified the artifact without redispatch.

The observable terminal record had 18 ordered events, one `EffectAcknowledged`, `receiverCallCount: 1`, `protectedResultReloaded: true`, `artifactVerified: true`, unauthenticated HTTP 401, and cross-tenant HTTP 404. This proves the tested registered effect strategy does not repeat an already acknowledged effect in this crash boundary. It is not a global exactly-once claim.

## Performance regression evidence

Measured at `2026-08-03T22:07:11.816Z` on Node `v22.22.3`, macOS arm64. Fixtures are deterministic and contain no customer/model/SaaS timing.

| Measurement               | Observation                                                                       | Regression budget                                 |
| ------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------- |
| Slow-consumer stream      | 10 MiB, 160 chunks, maximum queued 131,072 bytes, maximum RSS delta 819,200 bytes | Queue ≤262,144 bytes; RSS delta ≤67,108,864 bytes |
| Event projection          | 10,000 validated events, 211.375 ms, RSS delta 46,907,392 bytes                   | ≤10,000 ms; RSS delta ≤268,435,456 bytes          |
| Packed runtime quickstart | 148.068, 141.967, 143.266, 142.386, 140.032 ms; nearest-rank p90 148.068 ms       | p90 ≤10,000 ms                                    |

The packed measurement excludes dependency installation, initializer generation, network latency, and public registry access. It is not the PLAN.md initializer-to-first-run product SLO.

## Security, governance, and supply chain

The threat model records 18 high/critical risks with controls, tests, owners, and residual risk. Local gates passed 40 repository/release-boundary tests, 31 policy property/adversarial cases, 129 runtime command/crash cases, durable one-use authorization/approval/SecretRef claims, credential/model boundary canaries, and two incident drills. No verification exception or dependency-risk acceptance is recorded.

The sandbox reference fixture remains explicitly unsafe for production arbitrary-code isolation. Its final local image digest was `sha256:e56b4b73bc3dde378568919d66daace4a033968cf7981544708cc72fccd257d7`; it used digest-pinned `node:24.18.1-alpine@sha256:f70403e87646dc51b45295f4b8b70cdad0b63d2297c4c9899119b03f7af7a6b3`, UID/GID `65532:65532`, network `none`, read-only root, tmpfs workspace, zero mounts/socket, all capabilities dropped, `no-new-privileges`, 32 PIDs, 128 MiB, and 0.5 CPU. Secret/traversal/symlink/socket/loopback/metadata probes were denied; workspace/artifact operations succeeded; fork/loop/output attacks hit their bounds; temporary containers and host canary were removed.

The live npm advisory gate on 2026-08-04 initially found patched transitive findings in `undici` and `hono`. Narrow version-qualified workspace overrides moved them to 7.29.0 and 4.12.34; the repeated live audit then exited zero. The resulting offline npm advisory snapshot was retrieved at `2026-08-04T06:58:04.200Z`, is bound to the current lockfile, contains zero advisories, has canonical digest `sha256:ca749d5388483373b0e60052630bf90c6f3ee8d68107b1757b62f2d80be520b0`, and snapshot-file digest `sha256:9048127045a066027a2816f0612caf6709572f54f9a8ec11fc4c97d59de58c8d`. A new candidate lockfile must repeat the live gate; this time-bound result is not a permanent freshness claim.

License policy passed for 1,141 installed package versions in the canonical macOS workspace; the platform-extended Linux store reported 1,143. Workflow action refs were immutable; secret and guarded-release command audits passed. `pnpm docs:external-links:live` checked 65 current external URLs on 2026-08-04 and passed using bounded HEAD requests with GET fallback.

Authenticated GitHub API inspection at `2026-08-04T11:46:25Z` proved active, bypass-free `main-protection` ruleset `20377068` and `release-tag-protection` ruleset `20377070`. `main` requires pull requests, resolved conversations, linear history, denies force-push/deletion, and requires the four exact CI contexts from GitHub Actions integration `15368` with strict current-branch evaluation. `v*` tags deny update and deletion. CODEOWNERS validation returned zero errors; private vulnerability reporting and immutable releases both returned `enabled: true`; merge commits are disabled; merged branches are deleted. The `release` environment requires `fatihguner` approval and accepts only `main` branches or `v*` tags. Required PR/code-owner approval remains zero because `fatihguner` is currently the sole verified maintainer; this avoids a false independent-review claim and must be raised when a second maintainer joins.

## Documentation, accessibility, and localization

WP-09 is complete locally. `pnpm docs:build && pnpm docs:check` produced and validated 93 static HTML pages from a 61-page checked-copy manifest, including 13 required English/Turkish semantic mappings, generated API/package/LLM indexes, sitemap/search, reduced-motion support, resolved internal links, and zero serious/critical static accessibility findings.

Local Chromium checks covered 390×844 mobile, 720×900 reflow, and 1440×900 desktop at the deployed `/pactmark/` base. English/Turkish selection, document language, search-dialog semantics, internal base-path links, no horizontal overflow, and zero console warnings/errors were observed. The automation host did not apply true browser zoom, and no screen-reader run was performed; 200%/400% zoom, keyboard, and EN/TR screen-reader procedures remain explicit manual release checks in `docs/accessibility-and-localization.md`.

## Dry-run artifacts

The last canonical pre-record working-copy dry run produced:

| Artifact                                               | SHA-256/status                                                                                          |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| `.artifacts/release-dry-run/release-manifest.json`     | `5c0c2df0930abea24e07cb23a5b6ca666e6118399355c7902a8b391eed9b3642`                                      |
| `.artifacts/release-dry-run/source-manifest.json`      | `f17efb6013b7a8afa4cd35778f96566b8ae6680fb9ef46c74f950017de6d853d`                                      |
| `.artifacts/release-dry-run/pactmark-release.cdx.json` | `f49d9b63a8f76f3c4871a2b548119d61a032622bad7060853e0e7bf0492d0937`                                      |
| `.artifacts/release-dry-run/SHA256SUMS`                | `58b390336564a9674d0eb8075415a15571ebaf7736069378e46f38b560f58f4c`                                      |
| `.artifacts/release-dry-run/attestation-input.json`    | `prepared_not_attested`; file digest `f7e292a3859084ddad1fd95c3e5f4ee6752643d66bdfe739a082d47a7c57f453` |

These files were not signed, uploaded, or associated with a public release.

## Pending actions and required authority

| Pending action                                                            | Why pending                                                                                | Required authority/resource                                               |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| Run keyboard/screen-reader and true 200%/400% zoom gates                  | Static and local Chromium checks passed, but these assistive-technology checks did not run | Human reviewers and representative host/browser/AT combinations           |
| Repeat live advisories and external links on the frozen release candidate | Current live gates passed, but their results are intentionally time-bound                  | Network authorization on the exact candidate                              |
| Configure npm ownership/bootstrap/trusted publishing                      | No authenticated npm ownership or publisher exists                                         | Explicit release authorization, npm owner, 2FA/OIDC                       |
| Verify exact-tarball Vercel preview with durable TLS Postgres             | Local adapter/build evidence is not deployment evidence                                    | Vercel/TLS Postgres credentials, test principal, logs, teardown authority |
| Optionally verify Cloudflare staging                                      | Adapter remains experimental                                                               | Cloudflare credentials and deployment authorization                       |
| Publish npm `latest` and verify anonymous bytes                           | Irreversible external state and final step                                                 | Explicit user authorization after every preceding gate                    |

Until these actions are completed and evidenced, Pactmark may be described only as a public open-source implementation candidate. It must not be described as an npm release, production-verified, certified, fully secure, or exactly-once.
