# Pactmark v0.1 readiness record

Status: **v0.1.1 public package release verified**

Profile: `release` metadata; token-free trusted publication

Publication: `executed`

Record time: `2026-08-05T15:21:50Z`

This record preserves the historical v0.1 candidate evidence and records the completed v0.1.1 package release. It is not a security certification, production-deployment attestation, availability promise, or statement that Pactmark is production-ready.

## Current v0.1.1 release outcome

Protected release run [`31017765360`](https://github.com/pactmark/pactmark/actions/runs/31017765360) completed successfully for exact source commit `54fc96e870d73625c3b81e27f8aff490194c5876`. It passed `pnpm verify`, current network freshness gates, exact artifact checksum verification, GitHub build-provenance and SBOM attestation generation, source-bound npm OIDC authorization, publication, and frozen-candidate upload.

All 18 scoped packages and unscoped `create-pactmark` resolve `latest` to `0.1.1`. An independent anonymous check fetched every registry-served tarball, matched all 19 SHA-256 values to the frozen release manifest, verified `git+https://github.com/pactmark/pactmark.git` repository metadata, and found SLSA provenance records for every package. No long-lived npm token was created or retained.

Annotated tag [`v0.1.1`](https://github.com/pactmark/pactmark/releases/tag/v0.1.1) resolves to the exact release source. Its immutable GitHub Release contains 26 assets: 19 tarballs, checksums, source and release manifests, CycloneDX SBOM, attestation input, and GitHub provenance/SBOM bundles. The patch intentionally leaves the v0.1 runtime API and behavior unchanged.

## Candidate identity

The strongest local candidate evidence is the isolated `pnpm verify:clean-source` run completed at `2026-08-04T08:05:02.548Z` after the real GitHub repository metadata was packed across all 19 packages:

| Field                         | Value                                                                     |
| ----------------------------- | ------------------------------------------------------------------------- |
| Public source files           | 733                                                                       |
| Canonical source-copy digest  | `sha256:8b3de2d201501e39dc463eb5c16fcbcc86151573aff61391030cd819556134e8` |
| Synthetic baseline commit     | `88c351f960c7aeab50a5cbdca3f2550e98e7358f`                                |
| Synthetic baseline Git tree   | `0d893b3fa3f5438e006fdbe864e218ef204bca35`                                |
| Release source-tree digest    | `sha256:23840bdb63bf0fb30fe3a8799d732b15bb75d00ea61a4c602910d482e249bb89` |
| Clean release manifest digest | `sha256:dfd15b7caefbaae5211a9fb9b2ac1fd4cbfcc7106158f10b039b545261e47ee2` |
| Bootstrap log digest          | `sha256:f28e8aab10d6cfb79283e3faecc0f7144585fb2b409da444b72fe72fb4bfb538` |
| Full verification log digest  | `sha256:d947656bf3c9a10a051c9e7311a9a4bb5a575295eeaaca2d83ace890a8338c3d` |
| Consumer lock-template digest | `sha256:c14845d61517a71ba29cb1666a8e1440a5d9bec7ef88b44c0ed734e1fed75070` |
| Post-verification state       | `clean: true`; 19 packages                                                |

The synthetic commit exists only inside the disposable conformance checkout. It proves that the exact public export bootstraps and verifies from a clean source identity; it is not the maintainer commit, tag, signature, attestation, or frozen package-release candidate. Readiness evidence-record edits that describe the completed run follow that snapshot and receive targeted format/docs checks rather than retroactively changing its identity. After that proof, explicit authorization published root maintainer commit `3234ae5e0d5e7855d67aa3010cd2a12f88e86d3d` to `main` at exact origin `https://github.com/pactmark/pactmark.git`. The pre-commit local release manifest remains an honest snapshot with `clean: false`, `commit: uncommitted-local-source`, and `publication: not_authorized`; it is not relabeled after the fact.

The public repository was deliberately recreated under the same `pactmark/pactmark` name on 2026-08-05, with a new repository identity and a single reviewed public-source baseline. Release PR [#1](https://github.com/pactmark/pactmark/pull/1) passed the full host/runtime matrix in CI run [`31011208553`](https://github.com/pactmark/pactmark/actions/runs/31011208553) and the standalone deterministic-security workflow in run [`31011208473`](https://github.com/pactmark/pactmark/actions/runs/31011208473) before squash merge commit `c9b5eee7f98a31c17e1031accf9ec473cb1b65e9`. The npm organization and its 19 public `0.1.0` packages were not recreated or republished; their trusted-publisher coordinates remain `pactmark/pactmark`, `release.yml`, and environment `release`.

The pre-recreation protected candidate for maintainer commit `a39f77f1d306e64177fe7983a27b6eb06c73862d` built and attested all 19 `0.1.0` package tarballs. Its release-manifest SHA-256 is `4ee3ad64724a98084e07018ec8799159bb1c71fc39ffecc0ad87b6960d7bd328`, and those exact bytes passed the historical Vercel Preview gate described below. The old Actions run, tag, and GitHub Release were removed with the old repository; this record does not present them as live links. The independently verified npm `0.1.0` packages remain public.

## Toolchain and environment

| Component                                   | Exact value              |
| ------------------------------------------- | ------------------------ |
| Host                                        | `darwin arm64`           |
| Node.js used for canonical local run        | `v22.22.3`               |
| Supported release lines in package metadata | Node 22.14+ or Node 24.x |
| pnpm                                        | `11.18.0`                |
| TypeScript                                  | `6.0.3`                  |
| Vitest                                      | `4.1.10`                 |
| Turbo                                       | `2.10.8`                 |
| tsx                                         | `4.23.5`                 |
| ESLint                                      | `10.8.0`                 |
| Prettier                                    | `3.9.6`                  |
| API Extractor                               | `7.58.12`                |
| publint                                     | `0.3.22`                 |
| Are the Types Wrong                         | `0.18.5`                 |
| size-limit                                  | `13.0.3`                 |

The lockfile is the dependency-version authority. Its digest is `sha256:9269d84c439fbc3c0a942fba8d14cfcfbfe15af0ac234345c145e17530b751a5`.

## Canonical verification result

The exact aggregate command was:

```sh
pnpm verify
```

At `2026-08-04T08:05:02.548Z` it exited zero inside the clean export on Node `v22.22.3`/macOS arm64. It executed release-profile naming/repository consistency, manifest consistency, formatting, lint, strict typecheck, build, unit/integration/consumer/portability/example suites, documented commands, memory/Postgres contracts, migrations, two-process durable resume, PostgreSQL TLS, OCI conformance, Vercel/UI contracts, Cloudflare dry-run, performance, API/registration/dependency boundaries, unused/placeholder checks, adversarial security, offline advisory/license/workflow/secret/release audits, SBOM, the 93-page documentation build and checks, deterministic pack checks, and release dry-run.

The complete isolated clean-source log is retained with the digest above. Recreated-repository CI run [`31011208553`](https://github.com/pactmark/pactmark/actions/runs/31011208553) passed the canonical gates on Node 22.23.2 and 24.18.1 and the portable-host gates on Node 24.18.1 macOS and Windows; deterministic-security run [`31011208473`](https://github.com/pactmark/pactmark/actions/runs/31011208473) also passed. Authenticated inspection proved the repository protection/reporting controls described below. Manual screen-reader/keyboard/true-zoom checks remain open; package publication is complete.

## Package snapshot

All 19 frozen candidate packages expose the documented ESM entry with declarations. `@pactmark/cli` owns the `pactmark` binary and `create-pactmark` owns the initializer binary. The candidate pack rejected private inputs, environment files, tests, workspace links, coverage, and caches from tarballs.

| Package                            | Candidate tarball SHA-256                                          |
| ---------------------------------- | ------------------------------------------------------------------ |
| `@pactmark/agent`                  | `3f2efc2290b947edaf67bd8dd356fa305c47ff6f131a16cbd459dc3417104e59` |
| `@pactmark/ai-sdk`                 | `4b1bac5c38cc4a8f9131169a09ffa6d8550fe7e65209ed2d5c723f7531b41faa` |
| `@pactmark/cli`                    | `8a472a49a83f8aaa60bb6607aeced0b2f6a0a786be2a7ee564bf0b71e200e0bf` |
| `@pactmark/cloudflare`             | `0cd0474b376dbbdd196fa50a01378914c67872d4e0e6dfe7c5ed5cd1164267f1` |
| `@pactmark/core`                   | `2d885c87760f0ce2cf27be09e1bd4993bafb116719cfc3c71a1749e09f8f4553` |
| `@pactmark/driver-postgres-worker` | `39758b9714f44df884b5282cca5363909a9654db411fdac368795f27284ce41f` |
| `@pactmark/evidence`               | `ce1d11d439e54dc51cfcae4dee2f961bb8a521e7592261332f943f146c9b0454` |
| `@pactmark/executor-in-process`    | `619fc2be01c612b5defcddca9eece323f779479e356d3f12c7ac7f131eee406c` |
| `@pactmark/http`                   | `01dfadeb7f94a7d35e8ccd9d7925fca3a141d34755e11ca8c627b95399fe2084` |
| `@pactmark/mcp`                    | `988fc7421725275cbd99abc9a68b0cfa021910b0e47388e0b5d79d54be838d8e` |
| `@pactmark/node`                   | `9bd24f1651d5e003b82f9a5467507a32d2dc031dbec10aca48e3681e94b9658d` |
| `@pactmark/otel`                   | `ab7c5a99165d4255ef971b634e859fcaeab9ab6c39bf2e6b6e331b2c1da2f5be` |
| `@pactmark/policy`                 | `d6e539f6c132f7fd997d59898be4f050244124e7ee6b03ddb2f06f92bfc96b4b` |
| `@pactmark/runtime`                | `84e7b310b37535dc9ac88ab307e9f46502ff0864b053abcde84ed1a472f894bb` |
| `@pactmark/store-memory`           | `94c16eba02eef1636b710bb822586570ad2e2e2758f52bef56acec68da877eae` |
| `@pactmark/store-postgres`         | `b8f76d47d490f3301d6b8e88e3d637bc22a301535906088c6bab2cbe37213352` |
| `@pactmark/testing`                | `cd177fdf266486bb33e7c5ac93cfc3cf6c5bbad7f4f509cd96f7c673e644e7f6` |
| `@pactmark/vercel`                 | `10976cd8fb72757f2076af5fa490e2907fd4e991040488420c21e56b0e57dfa8` |
| `create-pactmark`                  | `f62bf497144e4c139d480689e0d083b8163f2a7b614f9357f5387d1e24756097` |

Independent consumers passed NodeNext and Bundler resolution from absolute tarballs. Every candidate was produced twice from canonical staging and required identical npm metadata and SHA-256 before acceptance. The loopback registry published exact candidates dependency-first and initializer-last, then ran the npm initializer without changing global npm configuration. Independent Yarn 4 and Bun consumers installed/imported the packed core candidate.

## Local compatibility matrix

| Dimension                                     | Status                          | Evidence boundary                                                                                                                                                                               |
| --------------------------------------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Node.js 22 on macOS arm64                     | `build-compatible`              | Canonical local build, tests, consumers, benchmarks, PostgreSQL, and Docker orchestration used Node `v22.22.3`.                                                                                 |
| Node.js 22.23.2 on Linux arm64                | `build-compatible`              | Digest-pinned Bookworm image passed the offline portable-host gate: 43 typechecks, 30 builds, 43 unit tasks, 8 consumers, and 19 portability tasks, with zero downloads.                        |
| Node.js 24.18.1 on Linux arm64                | `build-compatible`              | Digest-pinned Bookworm image passed the same offline portable-host gate and counts with zero package downloads.                                                                                 |
| Node.js 24.18.1 on macOS/Windows              | `build-compatible`              | Public run `31011208553` passed typecheck, build, unit, independent packed-consumer, and portability gates on macOS 14 and Windows.                                                             |
| Node server                                   | `build-compatible`              | Loopback HTTP/SSE and quickstart contracts passed locally.                                                                                                                                      |
| Node OCI image                                | `build-compatible`              | Offline host materialization, build network `none`, read-only runtime container, health/run/inspection, and cleanup passed locally. No registry or multi-arch claim.                            |
| Next.js/Vercel Node adapter                   | `build-compatible`              | Local build, routes, security, accessibility, and adapter contracts passed; the exact candidate also passed the live Preview host gate below.                                                   |
| Vercel Preview                                | `preview-deployment-verified`   | Exact attested tarballs reached a protected Ready Preview in `iad1`; URL/alias, health/readiness, SSE, auth denial, cancellation, durable resume, rollback, and logs passed.                    |
| Vercel Production                             | `unsupported`                   | No Production promotion, traffic verification, or production-support claim was authorized.                                                                                                      |
| Cloudflare Worker                             | `staging-verified-experimental` | Portable tests, generated types, Wrangler dry-run, live health/error routing, fail-closed readiness, and one deterministic SSE run passed on the ephemeral staging profile described below.     |
| Memory persistence                            | `build-compatible`              | Deterministic and tenant-scoped but explicitly ephemeral; not production durability.                                                                                                            |
| PostgreSQL durable profile                    | `preview-deployment-verified`   | Local PostgreSQL 17 gates passed; a test-only free Neon database additionally proved hostname-verified TLS, persisted checkpoint/resume, cancellation, and effect receipts from Vercel Preview. |
| npm initializer/local registry                | `build-compatible`              | Exact candidate publication and initializer execution passed against an ephemeral loopback registry.                                                                                            |
| Yarn 4/Bun packed library                     | `build-compatible`              | Independent packed-library install/import passed locally.                                                                                                                                       |
| Direct runtime, HTTP JSON/SSE, MCP stdio/HTTP | `build-compatible`              | Deterministic local contract evidence; no live provider/platform claim.                                                                                                                         |

The final OCI conformance run built with network disabled and produced local image digest `sha256:f8457be58ddda12745c93eeea0aca6211d75eb65491933fa5fe86672e4001dba`. The read-only container returned health 200, honestly returned readiness 503 for its ephemeral profile, completed a streamed run, persisted terminal inspection, failed terminal cancellation closed with `KAF_SCHEMA_INVALID`, exposed no secret-named environment value, and was removed.

## Live Cloudflare staging evidence

Authorized staging deployment `pactmark-staging` was created at `2026-08-05T16:12:10.482Z` on `https://pactmark-staging.pactmark.workers.dev` with Cloudflare version `0fb1e5ac-3dc2-4227-b78d-3e8554799c57`. The first live upload rejected module-global random-value generation with Cloudflare validation code `10021`; moving development authority/runtime initialization inside the first request removed that platform incompatibility. A repeated upload reported 82 ms startup and the deployment listing assigned 100% traffic to the recorded version.

After edge propagation, `/api/agent/healthz` returned 200 and `{"status":"ok"}`; an unknown route returned 404 with stable `KAF_HTTP_NOT_FOUND`. `/api/agent/readyz` intentionally returned 503: its capability report identified `executionProfile=ephemeral`, `durableStorage=false`, `protectedWorkOrders=false`, `sandbox=unsafe_local`, `networkPolicy=declared`, and no background wakeup. This is fail-closed evidence for the experimental preview, not production readiness.

External-effect-free run `run_msgaet0c_7f48f1f34746d15b42a5c685` returned HTTP 200 and 15 ordered SSE events from `RunAccepted` through `ToolCallCompleted`, `VerificationRecorded(status=pass)`, and `RunCompleted` at `2026-08-05T16:14:21.276Z`. The fixture uses anonymous-development authority, process-local memory, and a deterministic local model/tool; it contains no customer data, credential, durable resume proof, provider call, or production-isolation claim.

## Live exact-candidate Vercel Preview evidence

The standalone fixture vendored all 19 candidate tarballs and used a complete `pnpm.overrides` map to resolve every direct and transitive Pactmark dependency through `file:vendor`. Digest verification found zero mismatches, the frozen lockfile contained zero public-registry Pactmark entries, and the public-safe 55-file source tree plus 86-file prebuilt output had SHA-256 digests `040febbebc77150125f4416461af3c605c05c2594404bd213d2604b984451fc5` and `f6d6df52b7631bb994f311dfd32dd03482634b880199758a0fa6e1396641e230`.

Vercel deployment [`dpl_GEYURHYPNPHf14ERKo5Nmy6Tcm7W`](https://vercel.com/komunite/pactmark-v0-1-preview/GEYURHYPNPHf14ERKo5Nmy6Tcm7W) is Ready, targets Preview, runs in `iad1`, and is available at `https://pactmark-v0-1-preview-evs09g0t0-komunite.vercel.app` behind Vercel Deployment Protection. Health returned 200; readiness passed with durable storage, protected context/work orders, cancellation, and effect reconciliation. Authenticated SSE run `run_msevuoh1_bd28c94daced37013a5b4995` emitted 15 ordered events through `RunCompleted`; the same protected run surface returned 401 and `KAF_HTTP_AUTHENTICATION_REQUIRED` without the application bearer credential.

Durable run `run-b6f3ac25-0429-4000-843c-d9f7021810bf-1` checkpointed immediately after an acknowledged native effect and completed only after a separate authenticated resume request. The free-plan, test-only Neon database recorded checkpoint/resume timestamps, all 18 terminal events, the exact candidate identity, TLS mode `verify-full`, and one effect receipt with `call_count=1`. Independent run `run-8f923d21-7b51-4b61-aeb0-02b5697c941f-4` persisted `state=cancelled`, `RunCancelled`, and the same single-call receipt property. An isolated disposable alias successfully moved to a prior healthy Preview and back to the final deployment before deletion; six intermediate Preview deployments were removed. Final error-level runtime-log inspection found no entries. Current live npm advisory and external-link gates exited zero.

The Vercel Preview and free Neon database were test-only evidence surfaces with no customer or production data. After exact-target approval, the accidental Production-target deployment, retained Preview project, and test Neon projects were removed and verified absent. A subsequent handoff audit repaired the guarded publisher before v0.1.1; the historical Preview therefore remains evidence for the older exact candidate only and is not presented as v0.1.1 deployment proof.

## Durability and effect evidence

The canonical disposable PostgreSQL 17 gate applied migrations through 010 and passed the real database/worker/TLS suites. Its two-process scenario:

1. started a run over authenticated HTTP in process A;
2. used the sealed registered model path with a WorkOrder-postdated credential and durable model-call reservation;
3. dispatched and acknowledged a native governed effect;
4. killed process A with `SIGKILL`;
5. created a fresh process B/runtime/store/protector;
6. reloaded the protected result and completed/verified the artifact without redispatch.

The observable terminal record had 18 ordered events, one `EffectAcknowledged`, `receiverCallCount: 1`, `protectedResultReloaded: true`, `artifactVerified: true`, unauthenticated HTTP 401, and cross-tenant HTTP 404. This proves the tested registered effect strategy does not repeat an already acknowledged effect in this crash boundary. It is not a global exactly-once claim.

## Performance regression evidence

Measured at `2026-08-03T22:07:11.816Z` on Node `v22.22.3`, macOS arm64. Fixtures are deterministic and contain no customer/model/SaaS timing.

| Measurement               | Observation                                                                       | Regression budget                                 |
| ------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------- |
| Slow-consumer stream      | 10 MiB, 160 chunks, maximum queued 131,072 bytes, maximum RSS delta 819,200 bytes | Queue ≤262,144 bytes; RSS delta ≤67,108,864 bytes |
| Event projection          | 10,000 validated events, 211.375 ms, RSS delta 46,907,392 bytes                   | ≤10,000 ms; RSS delta ≤268,435,456 bytes          |
| Packed runtime quickstart | 148.068, 141.967, 143.266, 142.386, 140.032 ms; nearest-rank p90 148.068 ms       | p90 ≤10,000 ms                                    |

The packed measurement excludes dependency installation, initializer generation, network latency, and public registry access. It is not an initializer-to-first-run product SLO.

## Security, governance, and supply chain

The threat model records 18 high/critical risks with controls, tests, owners, and residual risk. Local gates passed 40 repository/release-boundary tests, 31 policy property/adversarial cases, 129 runtime command/crash cases, durable one-use authorization/approval/SecretRef claims, credential/model boundary canaries, and two incident drills. No verification exception or dependency-risk acceptance is recorded.

The sandbox reference fixture remains explicitly unsafe for production arbitrary-code isolation. Its final local image digest was `sha256:e56b4b73bc3dde378568919d66daace4a033968cf7981544708cc72fccd257d7`; it used digest-pinned `node:24.18.1-alpine@sha256:f70403e87646dc51b45295f4b8b70cdad0b63d2297c4c9899119b03f7af7a6b3`, UID/GID `65532:65532`, network `none`, read-only root, tmpfs workspace, zero mounts/socket, all capabilities dropped, `no-new-privileges`, 32 PIDs, 128 MiB, and 0.5 CPU. Secret/traversal/symlink/socket/loopback/metadata probes were denied; workspace/artifact operations succeeded; fork/loop/output attacks hit their bounds; temporary containers and host canary were removed.

The live npm advisory gate on 2026-08-04 initially found patched transitive findings in `undici` and `hono`. Narrow version-qualified workspace overrides moved them to 7.29.0 and 4.12.34; the repeated live audit then exited zero. The resulting offline npm advisory snapshot was retrieved at `2026-08-04T06:58:04.200Z`, is bound to the current lockfile, contains zero advisories, has canonical digest `sha256:ca749d5388483373b0e60052630bf90c6f3ee8d68107b1757b62f2d80be520b0`, and snapshot-file digest `sha256:9048127045a066027a2816f0612caf6709572f54f9a8ec11fc4c97d59de58c8d`. A new candidate lockfile must repeat the live gate; this time-bound result is not a permanent freshness claim.

License policy passed for 1,141 installed package versions in the canonical macOS workspace; the platform-extended Linux store reported 1,143. Workflow action refs were immutable; secret and guarded-release command audits passed. `pnpm docs:external-links:live` checked 65 current external URLs on 2026-08-04 and passed using bounded HEAD requests with GET fallback.

Authenticated GitHub API inspection at `2026-08-04T11:46:25Z` proved active, bypass-free `main-protection` ruleset `20377068` and `release-tag-protection` ruleset `20377070`. `main` requires pull requests, resolved conversations, linear history, denies force-push/deletion, and requires the four exact CI contexts from GitHub Actions integration `15368` with strict current-branch evaluation. `v*` tags deny update and deletion. CODEOWNERS validation returned zero errors; private vulnerability reporting and immutable releases both returned `enabled: true`; merge commits are disabled; merged branches are deleted. The `release` environment requires `fatihguner` approval and accepts only `main` branches or `v*` tags. Required PR/code-owner approval remains zero because `fatihguner` is currently the sole verified maintainer; this avoids a false independent-review claim and must be raised when a second maintainer joins.

Authenticated npm browser inspection at `2026-08-04T12:17:06Z` verified that owner `fatihguner` uses Komünite-branded package metadata, has 2FA enabled for authorization and publishing, and has one registered security key. The authorized workflow created organization `pactmark` on the free unlimited-public-package plan. The organization reports one owner, one 2FA-enabled member, zero 2FA-disabled members, active organization-wide 2FA enforcement, a default `developers` team, zero packages, and a `$0` monthly bill. No access token, package, package transfer, trusted publisher, or paid plan was created.

## Documentation, accessibility, and localization

WP-09 is complete locally. `pnpm docs:build && pnpm docs:check` produced and validated 93 static HTML pages from a 61-page checked-copy manifest, including 13 required English/Turkish semantic mappings, generated API/package/LLM indexes, sitemap/search, reduced-motion support, resolved internal links, and zero serious/critical static accessibility findings.

Local Chrome checks covered 390×844 mobile, 1440×900 desktop, and device-metric reflow surrogates at 720 CSS px/2× and 360 CSS px/4× at the `/pactmark/` base. Automated EN/TR keyboard traversal, localized skip-link order, accessibility-tree landmarks, named interactive controls, reduced motion, document language, search-dialog semantics, internal base-path links, no horizontal overflow, and zero console/page errors were observed. True browser-UI 200%/400% zoom and an auditory EN/TR screen-reader session remain explicit manual checks in `docs/accessibility-and-localization.md`; the device-metric and accessibility-tree results are not represented as those human checks.

## Published release artifacts

The immutable v0.1.1 GitHub Release contains:

| Artifact                    | SHA-256/status                                                     |
| --------------------------- | ------------------------------------------------------------------ |
| `release-manifest.json`     | `ef5b27560fec85e67293cf28091fc36e66c25022202ca71e2f30124d1763fa12` |
| `source-manifest.json`      | `0095aefd0be02f418d729cc811df7d91467074f433615ed65e6990217f900c67` |
| `pactmark-release.cdx.json` | `6cae27dbe08327ea838a7766e26cb73e8e0772ada2e1caf15d431493f7e2b7e0` |
| `SHA256SUMS`                | `064a994bf5be370c3148c8dd3fb480c4e3e74e3a1b9389204723287f2c249ed0` |
| `attestation-input.json`    | `05d261c2f532c5a379c92a7deb900e72d2fb7d555fa4502840b5b07c204dfe8f` |
| `provenance.jsonl`          | `bb0d8b1c84024aa535168c16dcf9273a1f4e423c2c3e04ac1b3e4909a7d21734` |
| `sbom.jsonl`                | `9a1a4029c0e473b056edec02dc60868a7dfaf004ee78632805be62cd2d491e2d` |

GitHub reports the Release as immutable. Its 19 package-asset digests match both `SHA256SUMS` and the anonymously fetched npm tarballs.

## Post-release experimental Executor gateway spike

At `2026-08-11T16:13:35Z`, the source-only Executor gateway spike completed its deterministic local evidence. The new `@pactmark/executor-sh` workspace is private and versioned `0.0.0-experimental.1`; it does not change the frozen set of 19 public packages and was not published or deployed. It exposes only individually pinned read tools through a host-owned guarded MCP connection. The generic Executor `execute` surface is not registered for model use. The adapter rejects writes, unknown risk metadata, unsupported isolation claims, schema drift, registration drift, connection-binding drift, malformed results, and cancellations with stable safe errors.

The commands below used Node `v22.22.3` with the exact pnpm executable prefix `/Users/cooper/.local/bin/node /Users/cooper/Library/pnpm/store/v11/links/@/pnpm/11.18.0/91401f657bea55d8d071c7d01eebc43eaf0dfa844b2ccdf47ce9666ff266f16c/node_modules/pnpm/bin/pnpm.cjs`:

| Exact command suffix                           | Observable proof                                                                                                            |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `--filter @pactmark/executor-sh typecheck`     | Production and test TypeScript projects passed.                                                                             |
| `--filter @pactmark/executor-sh test:coverage` | 2 files and 9 tests passed with 100% statements, branches, functions, and lines for the adapter production sources.         |
| `test:adapters`                                | 14/14 adapter tasks passed, including the real Pactmark MCP client composition over a deterministic in-memory wire fixture. |
| `lint`                                         | The workspace lint aggregate passed.                                                                                        |
| `typecheck`                                    | 45/45 workspace typecheck tasks passed across 32 packages.                                                                  |
| `build`                                        | 31/31 build tasks passed across 32 packages; the documentation build emitted 96 HTML pages.                                 |
| `registration:check`                           | 18 registration-source identities, including the Executor pin sources, matched their declared SHA-256 digests.              |
| `naming:check`                                 | Naming validation passed and the public package count remained 19.                                                          |
| `boundaries:check`                             | 657 modules and 1,356 dependencies passed with no dependency or portable-boundary violations.                               |
| `exec knip`                                    | No unused files or exports were reported; only existing informational configuration hints remained.                         |
| `docs:build`                                   | The static site built with valid internal links and zero serious/critical accessibility findings.                           |
| `docs:check`                                   | 63 checked source pages, 14 English/Turkish mappings, and 96 generated HTML pages passed.                                   |

The first restricted `test:adapters` attempt reached the existing `@pactmark/node` loopback-listener test and received the sandbox's `listen EPERM`. Repeating the same command with loopback permission passed all 14 tasks; this was an execution-environment denial, not a product-code failure.

This was the status at `2026-08-11T16:13:35Z`: `pnpm verify` was not run and no existing release gate was reopened or closed by the spike. At that time, a publishable or operational Executor integration still required a pinned self-hosted Executor build, verified telemetry-disabled behavior, a one-tenant instance, real read-only tool fixtures, authentication and egress canaries, packed-consumer and Node 24 evidence, and a refreshed live advisory snapshot. The production-hardening record below supersedes that historical status without changing its evidence.

## Post-release Executor production-hardening evidence

At `2026-08-11T17:30:01Z`, `@pactmark/executor-sh` was hardened as a private `0.1.0` integration. It remains outside the frozen set of 19 public packages and was neither published nor deployed. Construction now fails closed without a current digest-valid self-host conformance receipt and matching one-instance-per-tenant production profile. The profile binds the tenant, exact HTTPS origin, opaque connection, platform manifest, and receipt, and requires the recorded non-root/read-only, telemetry, local-network, stdio, volume, and backup controls. Receipt integrity is not a signature or remote attestation; target-specific production infrastructure remains separately owned.

The pinned upstream is Executor `v1.5.40`, source revision `b029643641832ef5f9b0d4ff263d96e1a5b2739c`, OCI index `sha256:3e9792043be7819361eada0c5c87ebfa66e996e15772f75a39aae76facd4cb88`, arm64 manifest `sha256:603522956d12788f9b50badd83e339ff5ab75486b4c16e4f10f46b6d0b49ee5b`, and amd64 manifest `sha256:2f6cc4e03470b1eca58f4cec08b99d3195fbffa07e4c626cf89cc328a74504d4`.

| Exact command                                                                                                                                          | Observable proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm --filter @pactmark/executor-sh test:coverage`                                                                                                    | 3 files and 13 deterministic tests passed; deployment admission, fail-closed tool binding, cancellation, timeout, malformed data, and safe errors were covered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `pnpm test:executor-sh-container`                                                                                                                      | Docker `29.3.1` ran the exact arm64 manifest with all 22 checks true: hardening/resources, persistence, stopped-volume restore, telemetry/analytics absence, outbound/private-network and stdio denials, unauthenticated denial, API-key and OAuth PKCE, cross-tenant denial, credential canaries, and the completed envelope. The receipt digest was `sha256:453fd43cbf1f40d283e724e65be9f6c6984c8bc9b098da365d7a0575257dc6b4`, valid from `2026-08-11T17:01:51.076Z` through `2026-08-18T17:01:51.075Z`; six exact containers, three data directories, and the network were cleaned.                                                            |
| `pnpm test:executor-sh-read-tools:live`                                                                                                                | A network-authorized hardened ephemeral instance registered, discovered, and invoked six GET-only public NPM API operations: point and range forms for last-day, last-week, and last-month downloads. All six returned HTTP 200 with three point and three range shapes; canaries were absent and cleanup passed. No SaaS credential or external write was used.                                                                                                                                                                                                                                                                                  |
| `pnpm test:executor-sh-packed` under Node `v22.22.3` and `v24.14.0`                                                                                    | Independent offline consumers installed only packed `@pactmark/core`, `@pactmark/mcp`, and `@pactmark/executor-sh` tarballs. Workspace links/private sources were absent and the deployment receipt/profile smoke passed on both supported release lines.                                                                                                                                                                                                                                                                                                                                                                                         |
| `pnpm audit:dependencies:refresh`                                                                                                                      | A live audit exposed transitive `nanoid` below `3.3.17`; the lock override was raised to `3.3.17`, then the refreshed snapshot recorded zero advisories. Snapshot digest: `sha256:8fddc05146cabebc167f8b1bc1b9da07222635e1ea4759ae4159d362b52b7ded`; lock digest: `sha256:84facedd02525b14d094dfd3ee37798f40e1cb23050c24288c2bbd2391136ec2`.                                                                                                                                                                                                                                                                                                      |
| `pnpm registration:check`                                                                                                                              | All 19 registration-source identities, including the hardened Executor adapter, deployment admission, and contracts, matched their declared SHA-256 digests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/pnpm/bin/pnpm.mjs verify` at `2026-08-11T17:46:19Z` | The canonical aggregate completed successfully: manifest/format/lint; 45 typecheck and 31 build tasks; unit, integration, consumer, portability, Postgres, container, UI, performance, API, boundary, placeholder, security, docs, pack, and release dry-run gates. The embedded Executor conformance again passed all 22 checks on the exact arm64 manifest, emitted receipt `sha256:4838511bacb7ede21484eb3f6f5db7177c0d7c85197cddfcb6ce7d0b25dd6a06` valid from `2026-08-11T17:45:07.200Z` through `2026-08-18T17:45:07.199Z`, and verified cleanup. Release dry-run inspected 779 source files and retained the frozen 19-package public set. |

The container and public-read gates establish bounded behavior for their exact local fixtures. They do not prove arbitrary-code isolation, container-escape resistance, production availability, or a separate deployment's TLS, encrypted storage, backup, egress, credential-rotation, and monitoring controls. Those controls and a fresh target-specific receipt remain mandatory before enabling a production tenant.

## Pending actions and required authority

| Pending action                                           | Why pending                                                                                | Required authority/resource                                     |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| Run keyboard/screen-reader and true 200%/400% zoom gates | Static and local Chromium checks passed, but these assistive-technology checks did not run | Human reviewers and representative host/browser/AT combinations |
| Establish a production deployment evidence boundary      | The verified package release is not a production-host or production-isolation claim        | Separate production authorization and executable evidence       |

Pactmark may be described as a public open-source npm release at v0.1.1. It must not be described as production-verified, certified, fully secure, or exactly-once.
