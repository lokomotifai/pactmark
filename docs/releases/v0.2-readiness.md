# Pactmark v0.2 readiness record

Status: **v0.2.0 public package release verified**

Profile: `release` metadata; token-free trusted publication

Publication: `executed through protected OIDC workflow`

This record tracks the v0.2.0 public release derived from the verified v0.1.2 public release.
It is not a security certification, production-deployment attestation, availability promise, or
claim that Pactmark is fully secure or production-isolated.

## Published release outcome

Protected release run [`31886596018`](https://github.com/lokomotifai/pactmark/actions/runs/31886596018)
completed successfully at `2026-08-15T14:42:31Z` for exact source commit
`f79611a4573e405fbd7e85699a861c5a087a766d`. The unprivileged job ran the canonical
`pnpm verify` aggregate and current network freshness gates, then built and checksum-verified the
frozen candidate. After explicit approval of the `release` environment, the separate job reverified
those bytes, generated GitHub build-provenance and SBOM attestations, and published through npm OIDC
trusted publishing without a long-lived npm token.

Independent anonymous verification then fetched all 19 registry-served `0.2.0` tarballs. Every
package resolved `latest` to `0.2.0`, exposed exact
`git+https://github.com/lokomotifai/pactmark.git` repository metadata and its real package directory,
matched the frozen release-manifest SHA-256, and returned an npm SLSA provenance record.

Annotated tag and immutable 26-asset GitHub Release
[`v0.2.0`](https://github.com/lokomotifai/pactmark/releases/tag/v0.2.0) resolve to the exact release
source. The assets comprise 19 tarballs, checksums, source and release manifests, a CycloneDX SBOM,
attestation input, and GitHub provenance/SBOM bundles.

Hosted release artifact digests are:

| Artifact                    | SHA-256                                                            |
| --------------------------- | ------------------------------------------------------------------ |
| `SHA256SUMS`                | `8677edcd1ae1443bbe75639832e17ed31a2cac3c2612bb3f934c60b9ec265773` |
| `release-manifest.json`     | `1f102acf17011d173538f7c4335875af89a64e89f41c29746734d76f6d95a2c9` |
| `source-manifest.json`      | `dfdff7e2cba213af3243bc7547aeb80668c6465e7655c2e206d57b4b39a27933` |
| `pactmark-release.cdx.json` | `25d64802d2c2ca9929fe5124c9e07ebb24b92cb976845ebc5ae1d37bd1ba490f` |
| `attestation-input.json`    | `d7ad752ee48eb22f80c7bcf1be0957cacb9e1fb72e8ac25a5bca8076299907f9` |
| `provenance.jsonl`          | `7206631adf42d76e8c3ac375c697c20ca1d10e51cea1df70d1068b4fd9d76703` |
| `sbom.jsonl`                | `054fd66e4fa65cece94c14f6a2f0a085e21c2a2a25f65832bbb8788ab7ce4b99` |

## Candidate scope

The coordinated public release contains 18 `@pactmark/*` packages and the unscoped
`create-pactmark` initializer, all at version `0.2.0`. The private `@pactmark/executor-sh` workspace
remains version `0.1.0`, is excluded from public release artifacts, and is neither an npm
publication target nor a production-deployment claim.

The release incorporates the authority-boundary and independent security-audit remediation already
recorded in the historical [v0.1 readiness record](./v0.1-readiness.md): host-derived tool identity,
complete fail-closed policy preflight, bound grants and approvals, effect and executor admission,
HTTP and MCP boundary hardening, tenant-aware PostgreSQL and worker access, credential and telemetry
redaction, portable evidence attestations, safer initialization, packed-consumer strengthening, and
immutable supply-chain workflows.

## Version and publication invariants

- Every public package manifest and packed internal dependency must resolve to exact `0.2.0`.
- The protected workflow accepts only `publish-pactmark-0.2.0` on `main` in
  `lokomotifai/pactmark`.
- Candidate construction runs without npm publication authority. The separate `release` environment
  job re-verifies the frozen artifact before OIDC attestation and publication.
- All 19 npm trusted publishers must remain bound to repository `lokomotifai/pactmark`, workflow
  `release.yml`, environment `release`, and GitHub-hosted execution.
- No long-lived npm token is required or permitted by the release workflow.
- The annotated `v0.2.0` tag and immutable GitHub Release may be created only for the exact source
  commit whose registry-served tarballs pass anonymous checksum and provenance verification.

## Local candidate verification

At `2026-08-15T12:41:54Z`, the prepared source completed the canonical local aggregate on Node
`v22.22.3`, macOS arm64, using pinned pnpm `11.18.0`:

```sh
/Users/cooper/.local/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs verify
```

The command exited zero. Observable proof included 46 successful typecheck tasks; 46 successful unit
tasks; 8 integration and 9 independent packed-consumer tests; a private Executor consumer bound to
public `@pactmark/core`, `@pactmark/policy`, and `@pactmark/mcp` `0.2.0` tarballs; PostgreSQL 17
durable crash/resume with 18 ordered events, one acknowledged effect, one receiver call,
unauthenticated `401`, and cross-tenant `404`; hostname-verified PostgreSQL TLS; OCI, unsafe-reference
sandbox, and production-guarded Executor conformance; Vercel and Cloudflare contract gates; 49
adversarial security cases; 34 canonicalization properties; 130 runtime crash/command cases; 1,205
license-checked installed package versions; immutable workflow, secret-history, guarded-release,
portable-boundary, API, registration, and public-surface audits; 63 checked documentation pages, 14
translation mappings, 96 generated HTML files, valid internal links, and zero serious/critical static
accessibility findings.

The release dry-run inspected 817 public source files and produced 19 packages, all exactly `0.2.0`,
from clean source commit `e00faaa0c2b46552594f188e3c1a953cfb4a73e8`. The draft manifest retained
`publication: not_authorized`. Local artifact digests were:

| Artifact                    | SHA-256                                                            |
| --------------------------- | ------------------------------------------------------------------ |
| `release-manifest.json`     | `9374a9d609879da1b5a771790108f731fa593de331297f2b777937f5f44b07f6` |
| `SHA256SUMS`                | `2970efe3184a5704cad4ca65b5dbcd96afd7eb2d761892fb48a08ab2d757d9cd` |
| `pactmark-release.cdx.json` | `b2af55e65f5e0d5ceb5a1c0efa550dab91bd9f22f8c2dca257110f9d0b6b58bd` |

These are local candidate artifacts, not the frozen GitHub-hosted publication artifacts. Hosted
construction must reproduce and independently verify its own exact bytes.

## Network-authorized freshness checks

At `2026-08-15T12:57:22Z`, the prepared candidate completed the two intentionally separate live
checks:

```sh
/Users/cooper/.local/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs audit:dependencies:live
/Users/cooper/.local/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs docs:external-links:live
```

Both commands exited zero. The npm advisory service reported `No known vulnerabilities found` at
the configured `high` threshold, and every checked external documentation link returned an accepted
live response. These results are point-in-time observations, not continuing vulnerability or link
availability guarantees.

## Hosted candidate verification

Pull request [#27](https://github.com/lokomotifai/pactmark/pull/27) verified final head
`2d76d3fd4bfd7892cb20bbd420131fefff06bf1d` on GitHub-hosted runners. At
`2026-08-15T13:11:35Z`, the required checks had completed as follows:

| Workflow          | Run           | Observable result                                            |
| ----------------- | ------------- | ------------------------------------------------------------ |
| CI                | `31886142715` | Node 22.23.2, Node 24.18.1, macOS 14, and Windows all passed |
| Fuzz              | `31886142754` | Jazzer.js security-boundary job passed                       |
| Security analysis | `31886142698` | CodeQL and the repository-owned OpenSSF Scorecard job passed |
| Security baseline | `31886142696` | Deterministic security job passed                            |

GitHub reported the pull request `MERGEABLE` with merge state `CLEAN`; its review-thread query
returned no unresolved threads. Rebase merge produced exact release source
`f79611a4573e405fbd7e85699a861c5a087a766d`, which the protected publication workflow independently
checked before building and publishing.

## Required release gates

| Gate                                           | Status   | Required evidence                                                                                      |
| ---------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------ |
| Coordinated versioning and changelogs          | Complete | 19 public packages at `0.2.0`; private workspaces excluded                                             |
| Deterministic local aggregate                  | Complete | `pnpm verify` exited zero on the prepared source                                                       |
| Current dependency and external-link freshness | Complete | Separate network-authorized npm-advisory and external-link checks exited zero                          |
| Hosted CI and security analysis                | Complete | Required Node 22/24, macOS, Windows, CodeQL, Scorecard, fuzz, and deterministic-security checks passed |
| Protected publication                          | Complete | Run `31886596018` published the exact frozen candidate through the approved OIDC environment           |
| Anonymous registry verification                | Complete | 19/19 `latest` versions, tarball checksums, repository metadata, and provenance records matched        |
| GitHub release identity                        | Complete | Immutable 26-asset `v0.2.0` release resolves to source `f79611a4573e405fbd7e85699a861c5a087a766d`      |

## Post-release gates outside package publication

| Gate                                                             | Current boundary                                                                                                         | Completion authority                                                                                                                |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| Keyboard, auditory screen-reader, and true 200%/400% zoom checks | Automated semantics and local Chromium checks are not substitutes for representative human assistive-technology sessions | Human reviewer with representative browser/AT combinations                                                                          |
| Production deployment evidence                                   | Package, container, Preview, and test-database evidence do not attest an arbitrary production host                       | Separately authorized production target, credentials, runtime role, TLS, storage, backup, egress, rotation, and monitoring evidence |

By `2026-08-15T14:47:46Z`, the release review had recorded a semantic inspection of the public
documentation that confirmed named landmark regions, heading structure, link/button names, and
alternative text in the browser accessibility snapshot. This is partial evidence only. The
available in-app browser did not expose reliable Tab focus traversal, the Chrome control surface was
unavailable, and macOS Computer Use permissions were not granted; representative keyboard,
VoiceOver, and true 200%/400% zoom sessions therefore remain open human-review gates.

Pactmark v0.2.0 may be described as a public open-source npm release with independently verified
registry bytes and provenance. It must not be described as certified, fully secure, exactly-once,
production-verified, or universally accessible from this evidence.

## Post-release documentation boundary transition

At `2026-08-15T15:34:38Z`, the repository's public product documentation boundary moved fully to
`https://pactmark-docs.lokomotif.ai`, whose source is maintained in the separate private
`lokomotifai/pactmark-documentation` repository. The duplicate Astro/Starlight application,
generated-site tooling, English/Turkish product-guide copies, and obsolete local navigation were
removed. ADRs, architecture rationale, contributor processes, security engineering records, and
release evidence remain in `docs/` as project records rather than as a second product
documentation site.

The already-disabled GitHub Pages surface had no Pages site and no `gh-pages` branch, but retained
11 historical `github-pages` deployment objects and a `github-pages` environment. Each exact
deployment ID was marked `inactive` and deleted, after which the environment was deleted. The
`release` environment was not changed.

| Exact command                                                                                                                                                                                                                                 | Observable proof                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gh api 'repos/lokomotifai/pactmark/deployments?environment=github-pages&per_page=100' --jq 'length'`                                                                                                                                         | Returned `0` after deleting deployment IDs `5919877437`, `5918408527`, `5910027933`, `5909052616`, `5908451757`, `5907466964`, `5864868721`, `5864085867`, `5766653120`, `5765690491`, and `5765448825`.                                                                    |
| `gh api repos/lokomotifai/pactmark/environments/github-pages`                                                                                                                                                                                 | Returned HTTP `404`; the `github-pages` environment no longer exists.                                                                                                                                                                                                       |
| `gh api repos/lokomotifai/pactmark/pages`                                                                                                                                                                                                     | Returned HTTP `404`; the repository has no GitHub Pages site.                                                                                                                                                                                                               |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs docs:check`                                                                              | The repository boundary check passed for six public entry files, rejected obsolete site paths, and validated all remaining local Markdown targets.                                                                                                                          |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs exec vitest run --config vitest.consumer.config.ts tests/consumer/root-contract.test.ts` | The root quality-command contract passed.                                                                                                                                                                                                                                   |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs audit:dependencies:offline`                                                              | The refreshed snapshot matched lockfile digest `sha256:f491219bf2541fd7b024e79063b965c1d2989d0f73af0fccfff3c5dd3dd70597` and reported zero advisories.                                                                                                                      |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs audit:licenses`                                                                          | The license policy passed for 1,205 installed package versions after removing the documentation dependency graph.                                                                                                                                                           |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs verify:manifest`                                                                         | The executable aggregate still matched the version-controlled gate manifest.                                                                                                                                                                                                |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs typecheck`                                                                               | All 45 package/workspace typecheck tasks passed on Node `v24.19.0`.                                                                                                                                                                                                         |
| `/Users/cooper/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/.pnpm/pnpm@11.18.0/node_modules/pnpm/bin/pnpm.cjs verify:ci`                                                                               | The complete CI aggregate exited zero at `2026-08-15T15:42:04Z`, including build, integration, packed-consumer, portability, examples, stores, migrations, API, naming, boundaries, Knip, 49 security cases, advisory, license, workflow, secret, and public-surface gates. |

A network-authorized `curl -L` check returned HTTP `200` for the canonical English home, Turkish
home, first-agent guide, and security-model guide.

## Unreleased host-boundary remediation

At `2026-08-23T15:40:34Z`, the post-`0.2.0` working tree completed a local remediation pass for
fail-closed host boundaries. This is unreleased evidence and does not change the immutable hosted
`v0.2.0` release record above.

The remediation requires explicit PostgreSQL tenant and purpose scopes, confines wildcard scope to
explicit local plaintext development, and sets `pactmark.tenant_id` inside package-owned tenant
transactions so the installed row-level-security policies participate in canonical storage paths.
It also adds a durable tenant-scoped `SecretRefStore` that persists only validated opaque reference
metadata and revocation state, never resolved credential values. Runtime, policy, and MCP adapters
now re-check versioned kill switches immediately before model, compensation, and MCP dispatch
boundaries. The policy adapter is named as a preflight engine while retaining a deprecated
compatibility alias; an allow result still does not mint executable authority. Error references now
resolve to the repository-owned error catalogue, and initializer documentation explicitly describes
the generated local sandbox and declared-network limitations.

The initial aggregate exposed two integration fixtures that had not declared their new storage
scope and a seven-day advisory snapshot that expired during the review. The fixtures were bound to
their exact test tenants and purposes. After explicit authorization, the dependency snapshot was
refreshed from npm, reported zero advisories, and was verified against the unchanged lockfile. No
release, publication, deployment, remote mutation, or production-readiness claim was made.

| Exact command                                             | Observable proof                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter @pactmark/policy test` | All 8 policy test files and 114 tests passed, including the fail-closed compensation strategy registration check.                                                                                                                                                                                               |
| `./node_modules/.bin/pnpm test:durable-resume`            | PostgreSQL 17 passed 11 storage integration tests and the worker integration test; the two-process resume probe completed with 18 ordered events, one acknowledged effect, one receiver call, protected-result reload, verified artifact, unauthenticated `401`, and cross-tenant `404`.                        |
| `./node_modules/.bin/pnpm test:postgres-tls`              | The real PostgreSQL 17 verify-full test passed with an exact-hostname trusted-CA connection while retaining the negative hostname and trust probes.                                                                                                                                                             |
| `./node_modules/.bin/pnpm audit:dependencies:refresh`     | The authorized npm audit export refreshed the snapshot at `2026-08-23T15:35:16.981Z` with zero advisories.                                                                                                                                                                                                      |
| `./node_modules/.bin/pnpm audit:dependencies:offline`     | Snapshot digest `sha256:3dd1590c3205fd843f367daf96e3caf6141ffb9dbc40c4d80f6dc30af2eb4d48` matched its checksum and lockfile digest `sha256:6d4e38f12a2140036f9f8645496522d3a692c5a11b6709ffbae3147966fc03c8`; zero advisories were present.                                                                     |
| `./node_modules/.bin/pnpm verify`                         | The complete release aggregate exited zero on Node `v22.22.3`: format, lint, typecheck, build, unit, integration, independent consumer, examples, stores, migrations, durable PostgreSQL, TLS, container, UI, performance, API, registration, boundary, security, docs, pack, and release dry-run gates passed. |

The final release dry-run inspected 769 public source files and assembled 19 packages without
publishing them. The current changeset records the intended package version impact; publication and
any hosted verification remain separately authorized future work.

## Unreleased local approval remediation

At `2026-09-04T10:36:20Z`, the working tree completed the seventh phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

The local facade now preserves `require_approval` as an explicit pending state
instead of silently denying it. Process-local challenges are one-use, and the
recorded decision is bound to the exact principal, tool, arguments, target,
human-readable preview, and policy decision before an explicit resume may
continue. Memory transactions roll approval claims back on failure. The
approval display is part of the preview digest, and preview producers are
responsible for excluding secrets. The static bearer fixture is deliberately
classified as `single_factor` and cannot satisfy R4; R3 compensation and R5
production presence remain host-provided boundaries.

| Exact command                                                               | Observable proof                                                                                                                                  |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter @pactmark/store-memory test`             | All 30 memory-store tests passed, including atomic one-use approval claims and rollback.                                                          |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime test`                  | Both runtime test files and all 153 tests passed, including exact approval binding, rejection, resume, and replay behavior.                       |
| `./node_modules/.bin/pnpm --filter pactmark-example-approval-purchase test` | All three purchase-boundary tests passed for approve, reject, explicit resume, and replay.                                                        |
| `./node_modules/.bin/pnpm --filter nextjs-vercel test`                      | All 11 fixture tests passed, including reachable challenge/decision routes, exact requested-item rendering, security defaults, and accessibility. |
| `./node_modules/.bin/pnpm --filter @pactmark/driver-postgres-worker test`   | Both worker test files and all 18 tests passed; delegated worker authority is represented conservatively as `single_factor`.                      |

The required independent review found two defects in the first green patch:
the UI rendered only opaque digests, and the purchase fixture ignored the
requested item. Both were fixed and covered before re-review; no remaining
Phase 7 security or correctness blocker was reported.

## Unreleased release and quality-gate remediation

At `2026-09-04T10:55:00Z`, all non-network Phase 8 implementation and local
verification completed. The aggregate remains blocked on a separately
authorized dependency-advisory refresh; this section is not a release-readiness
claim.

Package and protocol versions now derive from package metadata, Changesets and
protected release workflows derive one coordinated 19-package version, and CI
names the durable PostgreSQL, TLS, container, UI, and platform contracts. Knip
scans package sources, the obsolete size tool is gone, and the placeholder
scanner has executable tests for every marker. Repository file discovery now
excludes tracked files deleted in the working tree, preventing naming, source
manifest, and other scanners from attempting to read stale paths. Registration
source identities and public API reports were refreshed after the intended
contract changes.

| Exact command                                                                          | Observable proof                                                                                                                                                                 |
| -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm exec vitest run tests/unit/release-tooling.test.ts`          | All 17 release-tooling tests passed, including marker, version, workflow, and advisory-snapshot regressions.                                                                     |
| `./node_modules/.bin/pnpm exec vitest run tests/unit/repository.test.ts`               | All four repository-tooling tests passed, including exclusion of working-tree deletions from the public file set.                                                                |
| `./node_modules/.bin/pnpm release:version:check 0.2.0`                                 | The version contract returned `0.2.0` and the coordinated set of 19 publishable packages.                                                                                        |
| `./node_modules/.bin/pnpm verify:manifest`                                             | The executable aggregate matched the version-controlled gate manifest.                                                                                                           |
| `./node_modules/.bin/pnpm api:report && ./node_modules/.bin/pnpm api:check`            | The intended `RunFailed.errorCode` registry schema and storage-profile documentation were captured; all three API Extractor checks then passed.                                  |
| `./node_modules/.bin/pnpm registration:check`                                          | All 20 registration-source SHA-256 identities matched the refreshed manifest.                                                                                                    |
| `./node_modules/.bin/pnpm naming:check && ./node_modules/.bin/pnpm boundaries:check`   | Naming remained frozen at 19 publishable packages with no case collision; 684 modules and 1,477 dependencies had no boundary violation, and the portable import boundary passed. |
| `./node_modules/.bin/pnpm exec knip && ./node_modules/.bin/pnpm placeholders:check`    | Dead-code analysis exited zero; the placeholder/focused/skipped-test scan found no unexplained release-path finding.                                                             |
| `./node_modules/.bin/pnpm audit:licenses`                                              | The license policy passed for 808 installed package versions.                                                                                                                    |
| `./node_modules/.bin/pnpm audit:workflows && ./node_modules/.bin/pnpm audit:secrets`   | Workflow action references were immutable; repository and Git-history secret-pattern audits passed.                                                                              |
| `./node_modules/.bin/pnpm audit:release-commands`                                      | The guarded publisher remained the only executable release-mutation path.                                                                                                        |
| `./node_modules/.bin/pnpm public-surface:check && ./node_modules/.bin/pnpm docs:check` | The package public surface passed; six public documentation entry files passed their local boundary and link checks.                                                             |
| `./node_modules/.bin/pnpm sbom:generate && ./node_modules/.bin/pnpm release:dry-run`   | Each dry run inspected 775 source files, assembled all 19 packages, and generated a release manifest, CycloneDX SBOM, checksums, and attestation input without publishing.       |

The canonical `pnpm verify` run passed formatting, lint, the 47-task typecheck,
build, unit, integration, packed-consumer, platform, PostgreSQL 17, TLS,
container, UI, performance, and benchmark gates before exposing a stale API
report; that report was updated and `pnpm api:check` then passed. The remaining
security chain passed 49 security cases, 34 canonicalization properties,
authorization/crash/credential/admission suites, MCP sandbox contracts, and
both hardened container conformance probes before correctly stopping at
`KAF_ADVISORY_LOCKFILE_MISMATCH`.

The current lockfile digest is
`sha256:743d717c4c874619876cfde0ad3e9094df2b3840d354c45adb1690c86e4a233e`;
the last authorized snapshot is bound to
`sha256:6d4e38f12a2140036f9f8645496522d3a692c5a11b6709ffbae3147966fc03c8`.
The mismatch is intentional fail-closed behavior. Refreshing the snapshot sends
dependency-graph and lockfile-derived metadata to npm's advisory endpoint and
has not been authorized. Consequently, neither `pnpm security:verify` nor
`pnpm verify` is recorded as passing for this working tree.

## Unreleased fast-uri advisory remediation

At `2026-09-05T08:48:49Z`, the authorized advisory remediation and complete
local release aggregate passed. This is unreleased branch evidence and does
not change the immutable hosted `v0.2.0` record.

Four high-severity advisories affected the production dependency path
`@pactmark/mcp -> @modelcontextprotocol/sdk -> ajv -> fast-uri@3.1.5`.
The workspace now applies the repository-native, version-qualified
`fast-uri@<3.1.6 -> 3.1.6` security floor. The independent packed-consumer
generator and its frozen lock fixture carry the same exact resolution, and
regeneration also reconciled two stale `@pactmark/policy` tarball edges with
the current package manifests. No `fast-uri@3.1.5` resolution remains in
either lock artifact.

| Exact command                                                                                                                  | Observable proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm install --lockfile-only --frozen-lockfile=false`                                                     | All 33 workspace projects resolved successfully and the root lockfile selected `fast-uri@3.1.6` for both Ajv dependency paths.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `./node_modules/.bin/pnpm install --frozen-lockfile`                                                                           | The installed graph matched the updated lockfile and materialized `fast-uri@3.1.6`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `node --import tsx tooling/consumer/generate-lock-template.mts`                                                                | The independent packed-consumer lock template was regenerated with the exact `fast-uri@3.1.6` override and integrity.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `./node_modules/.bin/pnpm --filter @pactmark/mcp typecheck`                                                                    | The MCP package TypeScript project completed with no errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `./node_modules/.bin/pnpm --filter @pactmark/mcp build`                                                                        | The MCP package built its Node 22 ESM output, declarations, and source maps successfully.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `./node_modules/.bin/pnpm --filter @pactmark/mcp exec vitest run tests/adapter.test.ts tests/http.test.ts tests/stdio.test.ts` | All three focused files and 38 tests passed across MCP discovery, HTTP, stdio, schema, and transport behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `./node_modules/.bin/pnpm exec vitest run --config vitest.consumer.config.ts tests/consumer/packed-artifacts.test.ts`          | All six packed-artifact tests passed, including both frozen offline NodeNext and Bundler consumer installations. The first focused run exposed a missing fixture-side override copy; that representation was aligned before the passing rerun.                                                                                                                                                                                                                                                                                                     |
| `./node_modules/.bin/pnpm audit:dependencies:refresh`                                                                          | The explicitly authorized npm audit export refreshed the snapshot at `2026-09-05T08:42:07.359Z` with zero high-or-critical findings.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `./node_modules/.bin/pnpm audit:dependencies:offline`                                                                          | Snapshot digest `sha256:320f4253e5f4672fa7704a4d4246a4673125656ee6482f61c79e8e215a0abbf8` matched its checksum and lockfile digest `sha256:713cdf70ec7de13df056c3247c19f2f756e8cfb24d91b6a557f0fabc2e2ccc15`; the snapshot contained zero blocking advisories.                                                                                                                                                                                                                                                                                     |
| `./node_modules/.bin/pnpm audit:dependencies:live`                                                                             | The live npm gate exited zero at the configured high-severity threshold. It separately reported two moderate findings; this record does not claim a vulnerability-free graph.                                                                                                                                                                                                                                                                                                                                                                      |
| `./node_modules/.bin/pnpm verify`                                                                                              | The canonical aggregate exited zero with the required local loopback and Docker access. It passed manifest, formatting, lint, 47-task typecheck/build/unit suites, integration and independent consumer acceptance, portability/examples, PostgreSQL 17 durability and TLS, container/MCP/Executor isolation contracts, UI/platform/performance gates, API/registration/naming/boundary checks, 49 adversarial security cases, refreshed offline advisory and supply-chain audits, docs/public-surface checks, and the 19-package release dry run. |

The required independent pre-patch investigation confirmed that the vulnerable
parser was reachable from untrusted MCP `tools/list` output schemas before
Pactmark compared the host pin. The required independent patch review found no
surviving `3.1.5` resolution, override-to-use gap, generated-fixture drift, or
compatibility regression. Ajv accepts the patched release within its existing
`^3.0.1` range, so the MCP SDK and its public behavior remain unchanged.

No release, publication, deployment, or production-readiness claim was made
from this remediation.

## Unreleased user-surface and claim remediation

At `2026-09-04T10:55:00Z`, the ninth-phase user surfaces and tenth-phase claim
dispositions were completed locally. This remains unreleased evidence.

Generated projects now register and exercise a real governed R1 read tool, and
the documented event trail is tested. The portable example executes a real
local runtime and exposes honest Node, Vercel, and Cloudflare-compatible entry
signatures. The quickstart includes an AI SDK v7 live-provider smoke path that
requires an explicit opt-in and provider credential and is excluded from
required CI. The Next.js fixture documents its production-shaped fail-closed
default. Local English and Turkish security documentation now presents matching
event, approval, and safety claims.

`RunFailed.payload.errorCode` accepts registered KAF members rather than an
arbitrary matching string. PostgreSQL deletion support is documented at the
record-class boundary: lifecycle-managed protected WorkOrders, inputs,
contexts, and artifacts are deletable, while append-only run truth and
immutable evidence are not advertised as generally deletable. The threat model
describes the actual memory-store contract suite and the separate PostgreSQL
unit and real-role integration coverage. Host-provided signing, default-deny
egress, founder-led governance, package count, thin adapters, and runtime size
are recorded as trust, governance, or maintainability facts rather than
invented authorization defects.

| Exact command                                                            | Observable proof                                                                                        |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter create-pactmark test`                 | All 32 initializer tests passed, including the governed-tool generated event trail.                     |
| `./node_modules/.bin/pnpm --filter pactmark-example-portable-agent test` | Both portable-example scenarios passed through the real local runtime.                                  |
| `./node_modules/.bin/pnpm --filter pactmark-example-quickstart test`     | All three quickstart tests passed, including refusal to run the provider smoke without explicit opt-in. |
| `./node_modules/.bin/pnpm --filter nextjs-vercel test`                   | All 11 Next.js tests passed across routes, security defaults, and accessibility.                        |
| `./node_modules/.bin/pnpm test:example-readmes`                          | All 26 documented example commands executed successfully.                                               |
| `./node_modules/.bin/pnpm --filter @pactmark/core test`                  | All nine core files and 59 tests passed, including rejection of an unregistered `RunFailed` code.       |
| `./node_modules/.bin/pnpm lint && ./node_modules/.bin/pnpm typecheck`    | Workspace lint passed; all 47 typecheck tasks completed without error.                                  |
| `./node_modules/.bin/pnpm test:unit`                                     | All five root files and 37 tests passed; all 47 package test/build tasks passed.                        |

The ICLA date conflict is not safe to rewrite in place. It remains blocked on
counsel-approved replacement text, a new immutable document and signature, and
separate remote workflow authority. Runtime decomposition and package
consolidation remain ADR and semver work. No live provider key was supplied, so
the optional smoke was not run. No release, publication, deployment, push, or
remote setting was performed.

## Unreleased delegated run-scope remediation

At `2026-09-04T07:14:30Z`, the working tree completed the first phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

The runtime now enforces an authority's exact `runId` and stored `workOrderId`
before a scoped operation can observe or mutate a run. Scoped authority cannot
create a new run, and scoped `system_worker` authority is limited to the
execute, projection-read, and event-read operations used by the durable worker.
Stored input replays and committed command replays perform the scope check
before returning cached results, closing paths that would otherwise bypass a
deferred transaction callback. Unscoped host and user authority behavior is
preserved.

| Exact command                                                                                                                                                                     | Observable proof                                                                                                                          |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime exec vitest run tests/runtime.test.ts -t 'confines run-scoped authority\|protects typed input, commits one reference event'` | Both focused tests passed, including same-run execution, cross-run denial, worker operation denial, wrong-work-order input replay denial. |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime test`                                                                                                                        | Both runtime test files and all 138 tests passed.                                                                                         |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime typecheck`                                                                                                                   | The runtime TypeScript project completed with no errors.                                                                                  |
| `./node_modules/.bin/pnpm --filter @pactmark/driver-postgres-worker test`                                                                                                         | Both PostgreSQL worker test files and all 17 tests passed.                                                                                |
| `./node_modules/.bin/pnpm --filter @pactmark/driver-postgres-worker typecheck`                                                                                                    | The PostgreSQL worker TypeScript project completed with no errors.                                                                        |

No release, publication, deployment, remote mutation, or production-readiness
claim was made from this phase.

## Unreleased parser, identity, and adapter-boundary remediation

At `2026-09-04T09:28:54Z`, the working tree completed the sixth phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

Canonical JSON parsing now rejects integer-valued wire tokens that JavaScript
would round to a different canonical decimal value while accepting the
canonical writer's exponent output. Process-local tenant/run/resource keys use
canonical tuple identities across runtime cancellation and model calls,
policy grants and reservations, admission and credential stores, facade and
evidence registries, and PostgreSQL worker delegation leases. Tenant identity
is explicit on identifier-only memory and PostgreSQL UoW methods.

HTTP mutation bodies are parsed through their public runtime schemas and the
normalized value is used for both command digest and invocation, yielding a
stable `400 KAF_SCHEMA_INVALID` instead of adapter-dependent validation faults.
MCP output-schema absence has its own pin identity and permits valid text-only
content; a declared schema remains strict. AI SDK no-tool streams now surface
provider error and abort outcomes. Authorization reservations written with new
effects are atomically marked consumed; the ledger still accepts the legacy
durable `reserved` shape needed to resume v0.2 prepared effects. `egressBroker`
and model `targetDigest` retain compatibility as deprecated/legacy inputs, and
the facade no longer advertises unconsumed tool-credential authority.

| Exact command                                                                                                                                                                                                                                                                                                               | Observable proof                                                                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter @pactmark/core test`                                                                                                                                                                                                                                                                     | All 58 core tests passed, including canonical-number and tuple-registration regressions.                                                                                |
| `./node_modules/.bin/pnpm --filter @pactmark/policy test`                                                                                                                                                                                                                                                                   | All 118 policy tests passed, including equal-ID and delimiter-collision isolation for grants, admission, authorization reservations, and credential claims.             |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime test`                                                                                                                                                                                                                                                                  | Both runtime test files and all 153 tests passed, including tenant-qualified cancellation/model-call state, normalized approval state, and legacy authorization resume. |
| `./node_modules/.bin/pnpm --filter @pactmark/http test`                                                                                                                                                                                                                                                                     | All 15 HTTP tests passed, including malformed/overflow mutation bodies, stable schema errors, normalized command digests, and cancellation invocation.                  |
| `./node_modules/.bin/pnpm --filter @pactmark/mcp test`                                                                                                                                                                                                                                                                      | All 46 MCP tests passed, including absent-schema text output, declared-schema validation, malformed content, and schema add/remove drift.                               |
| `./node_modules/.bin/pnpm --filter @pactmark/ai-sdk test`                                                                                                                                                                                                                                                                   | All 14 AI SDK tests passed, including no-tool provider error and abort propagation.                                                                                     |
| `./node_modules/.bin/pnpm --filter @pactmark/store-memory test`                                                                                                                                                                                                                                                             | All 29 memory-store tests passed, including explicit-tenant UoW checks and legacy/new authorization lifecycle acceptance.                                               |
| `./node_modules/.bin/pnpm --filter @pactmark/store-postgres test`                                                                                                                                                                                                                                                           | All 75 PostgreSQL-store unit tests passed, including explicit-tenant UoW checks and legacy/new authorization lifecycle acceptance.                                      |
| `./node_modules/.bin/pnpm --filter @pactmark/evidence test && ./node_modules/.bin/pnpm --filter @pactmark/driver-postgres-worker test && ./node_modules/.bin/pnpm --filter @pactmark/agent test && ./node_modules/.bin/pnpm --filter @pactmark/executor-sh test && ./node_modules/.bin/pnpm --filter @pactmark/create test` | Evidence 16, worker 18, agent 21, shell executor 13, and initializer 31 tests passed.                                                                                   |
| `./node_modules/.bin/pnpm typecheck`                                                                                                                                                                                                                                                                                        | The complete 47-task workspace typecheck, including dependency builds and the Next.js type generation step, completed with no errors.                                   |

The required independent current-tree review reran 488 focused tests and all
ten scoped package typechecks after the final tuple-hardening delta. It found
no remaining reportable security or correctness blocker. Residual boundaries
are documented rather than overstated: strict parser callers still own byte
limits before exponent normalization, and the AI abort regression covers an
abort-induced stream failure while the explicit abort-part branch remains a
direct mapping.

No release, publication, deployment, remote mutation, or production-readiness
claim was made from this phase.

## Unreleased PostgreSQL retention and role-isolation remediation

At `2026-09-04T08:30:15Z`, the working tree completed the fifth phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

Protected-record stores now expose `purgeExpiredForTenant(tenantId)`, which
validates the tenant, includes it in the delete predicate, and installs the
transaction-local RLS context. Deprecated ambiguous `purgeExpired()` methods
fail closed. Cross-tenant expiry is isolated behind
`PostgresRetentionMaintenance` and an explicitly marked maintenance database;
that marker is a host assertion and never elevates PostgreSQL privileges. RLS
is enabled without `FORCE` so the documented table-owner maintenance role can
operate globally, while application and worker connections remain non-owner
`NOBYPASSRLS` roles.

Migration `012` separates expirable protected WorkOrder rows from immutable
run identity. New run bindings still require an exact WorkOrder parent and
matching work-order/execution-definition digests under a key-share lock.
Accepted run bindings remain immutable after the protected WorkOrder expires,
and wakeups reference those bindings, so expiry cannot erase run truth or
scheduler identity.

| Exact command                                                                  | Observable proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `./node_modules/.bin/pnpm --filter @pactmark/store-postgres test`              | All 12 unit test files and 73 tests passed, including migration `012`, tenant predicates, fail-closed deprecated methods, and maintenance-boundary coverage.                                                                                                                                                                                                                                                                                                                                                             |
| `./node_modules/.bin/pnpm --filter @pactmark/store-postgres typecheck`         | The PostgreSQL store TypeScript project completed with no errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `./node_modules/.bin/pnpm --filter @pactmark/store-postgres build`             | The package built its Node 22 ESM output, declarations, and source maps successfully.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `/Users/cooper/.local/bin/node --import tsx tooling/test-postgres-service.mts` | PostgreSQL 17 passed all 12 store integration tests and the worker integration test. The retention regression used a real non-owner `NOBYPASSRLS` role, deleted linked tenant A and global tenant B WorkOrders while retaining both immutable run/wakeup bindings, and rejected a new orphan binding. The two-process durable-resume probe also completed with 18 ordered events, one acknowledged effect, one receiver call, protected-result reload, verified artifact, unauthenticated `401`, and cross-tenant `404`. |

The required independent read-only patch review found that the first green
patch still attempted to delete WorkOrders referenced by immutable foreign
keys. That real PostgreSQL lifecycle defect was closed with migration `012` and
the linked-WorkOrder regression above. Re-review found no remaining scoped
cross-tenant, role, SQL, marker, or cleanup issue.

No release, publication, deployment, remote mutation, or production-readiness
claim was made from this phase.

## Unreleased terminal failure taxonomy remediation

At `2026-09-04T08:09:38Z`, the working tree completed the fourth phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

Once a run is accepted, deterministic run-owned model, tool, policy, effect,
verification, and evidence failures now append `RunFailed` with a registered
KAF code and safe details. Cancellation appends `RunCancelled`, including the
model-less compensation path. Unknown external outcomes and invalid or
throwing retry classifiers suspend the run instead of proving a false terminal
outcome. Lease contention, storage concurrency, simulated commit-boundary
crashes, and persisted AAD/binding drift remain nonterminal host/operator
failures. Malformed model emissions, policy decisions, verifier results, and
evidence records are schema- and binding-validated before they can affect run
truth. The local facade retains rejected background executions until `wait()`
observes them, closing the prior deletion race.

| Exact command                                                                                                                                                                                                                                                                       | Observable proof                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime exec vitest run tests/runtime.test.ts -t "stable terminal\|classifier\|malformed policy\|malformed .* result\|uncertain model dispatch\|separately authorized model-less compensation\|mismatched AAD\|lease for exclusivity"` | The 13 focused tests passed, covering terminal model/tool/policy/verifier/evidence failures, invalid and throwing classifiers, uncertainty parking, compensation cancellation, AAD integrity, and lease contention. |
| `./node_modules/.bin/pnpm --filter @pactmark/agent exec vitest run tests/agent.test.ts -t "background infrastructure failure"`                                                                                                                                                      | The focused facade test proved that a background infrastructure rejection remains observable through a later `wait()`.                                                                                              |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime test`                                                                                                                                                                                                                          | Both runtime test files and all 151 tests passed.                                                                                                                                                                   |
| `./node_modules/.bin/pnpm --filter @pactmark/agent test`                                                                                                                                                                                                                            | The agent test file and all 21 tests passed.                                                                                                                                                                        |
| `./node_modules/.bin/pnpm --filter @pactmark/core test`                                                                                                                                                                                                                             | All nine core test files and all 56 tests passed with the expanded stable error registry.                                                                                                                           |
| `./node_modules/.bin/pnpm --filter @pactmark/core typecheck && ./node_modules/.bin/pnpm --filter @pactmark/runtime typecheck && ./node_modules/.bin/pnpm --filter @pactmark/agent typecheck`                                                                                        | All three TypeScript projects completed with no errors.                                                                                                                                                             |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime build`                                                                                                                                                                                                                         | The runtime package built its ESM output, declarations, and source maps successfully.                                                                                                                               |

The required independent read-only patch review found three sibling boundary
gaps after the first green suite: malformed policy results could fall through
as allow, classifier faults were trusted, and verifier/evidence adapter results
were not runtime-validated. Each was fixed and added to the focused regression
set before this phase was closed.

No release, publication, deployment, remote mutation, or production-readiness
claim was made from this phase.

## Unreleased native effect replay remediation

At `2026-09-04T07:42:22Z`, the working tree completed the third phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

An existing `native + dispatched` effect now becomes uncertain and suspends
instead of being dispatched again merely because the strategy is named
`native`. The same conservative recovery remains in force for `none`.
Reconcilable retry remains available only after a validated `not_applied`
lookup. Before retry, the runtime binds the durable record's strategy,
strategy-registration digest, and operation key to the live registration,
persists a checkpointed `EffectDispatched` attempt, and uses an
attempt-qualified active-execution reservation key. Malformed lookup status can
no longer fall through to dispatch.

| Exact command                                                                                                                                                                                                                                                                                                                                               | Observable proof                                                                                                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime exec vitest run tests/runtime.test.ts -t 'parks a native effect after a dispatch-boundary crash\|records a fresh dispatch attempt after reconciliation\|runs reconcilable lookup before retry\|parks a none-strategy effect after a dispatch-boundary crash\|rejects malformed reconciliation status'` | All five focused regressions passed: native/none park, malformed and drifted recovery data fail closed, and a real crash with a prior active reservation records and settles only attempt two after `not_applied`. |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime test`                                                                                                                                                                                                                                                                                                  | Both runtime test files and all 141 tests passed.                                                                                                                                                                  |
| `./node_modules/.bin/pnpm --filter @pactmark/runtime typecheck`                                                                                                                                                                                                                                                                                             | The runtime TypeScript project completed with no errors.                                                                                                                                                           |
| `./node_modules/.bin/pnpm exec prettier --check packages/runtime/src/effects.ts packages/runtime/src/runtime.ts packages/runtime/tests/runtime.test.ts`                                                                                                                                                                                                     | All changed runtime files matched the repository's Prettier style.                                                                                                                                                 |
| `git diff --check`                                                                                                                                                                                                                                                                                                                                          | The working-tree patch had no whitespace errors.                                                                                                                                                                   |

An independent read-only security review additionally traced both normal and
interrupted recovery, challenged strategy and operation-key drift, validated
all reconciliation statuses, and checked the production-shaped
active-execution path. It found no remaining scoped bypass or legitimate
regression.

No release, publication, deployment, remote mutation, or production-readiness
claim was made from this phase.

## Unreleased production input-schema remediation

At `2026-09-04T07:25:03Z`, the working tree completed the second phase of the
source-backed roast remediation plan. This is unreleased local evidence and
does not change the immutable hosted `v0.2.0` record.

Production and local facade starts now validate WorkOrder input against the
exact executable schema retained for the compiled agent. The schema identity
must equal the definition's advertised `inputSchemaDigest`; drift fails closed.
Invalid values are reported as stable `KAF_SCHEMA_INVALID` errors before kernel
admission or persistence. Validation is intentionally non-transforming at this
boundary: the parsed value is discarded, and the original request remains the
authoritative input for command-digest verification and durable storage.

| Exact command                                                                                                             | Observable proof                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `./node_modules/.bin/pnpm --filter @pactmark/agent exec vitest run tests/agent.test.ts -t 'has no hidden local fallback'` | The focused production composition test passed; malformed input never entered the transaction and valid input reached it. |
| `./node_modules/.bin/pnpm --filter @pactmark/agent typecheck`                                                             | The agent TypeScript project completed with no errors.                                                                    |
| `./node_modules/.bin/pnpm --filter @pactmark/agent test`                                                                  | The agent test file and all 20 tests passed, including local-facade behavior.                                             |
| `./node_modules/.bin/pnpm --filter @pactmark/agent build`                                                                 | The package built declarations, ESM output, and source maps successfully.                                                 |
| `./node_modules/.bin/pnpm exec prettier --check packages/agent/src/runtime.ts packages/agent/tests/agent.test.ts`         | Both changed source files matched the repository's Prettier style.                                                        |

An independent read-only security review additionally verified that the
WeakMap-retained schema is bound to the exact compiled and frozen definition,
the validation occurs before `kernel.start`, and the discarded parsed output
cannot alter replay identity.

No release, publication, deployment, remote mutation, or production-readiness
claim was made from this phase.
