engine-strict=true
fund=false
# Install-time audit is disabled for deterministic/offline installs. `pnpm security:verify`
# checks the lockfile-bound advisory snapshot; the protected release workflow separately runs
# `pnpm audit:dependencies:live` before publication.
audit=false
strict-peer-dependencies=true
save-exact=true
