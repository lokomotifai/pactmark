# @pactmark/otel

Opt-in, metadata-only telemetry for Pactmark. A clean install configures no exporter and performs no network calls. Hosts explicitly provide an export callback and retain responsibility for transport, retention, and access controls.

Prompt, completion, tool input/output, artifact content, user identity, and secrets are not accepted by the default allowlist.
