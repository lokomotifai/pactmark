# @pactmark/cloudflare (experimental)

Experimental Module Worker/Web API translation over `@pactmark/http`. The base adapter does not require `nodejs_compat`, does not treat Worker memory as durable, and advertises no background wake-up or isolated egress. Templates pin compatibility date `2026-08-03`; updating it requires the full portability and security contract suite.
