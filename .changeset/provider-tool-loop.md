---
"@pactmark/agent": minor
"@pactmark/ai-sdk": minor
"@pactmark/core": minor
"@pactmark/runtime": minor
---

Add the real-provider tool loop, schema-only provider tool projection, model-bound agent context, and
the local facade defaults for governed reads and R2 writes.

Fail closed when distinct executable definitions alias one registration identity, and preserve the
accepted tenant, run, purpose, and data classification through effect and egress execution.
