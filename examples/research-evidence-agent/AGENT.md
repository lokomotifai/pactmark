# Research evidence agent

R1-only. The required path reads immutable embedded sources and emits title/body/citations with source observation dates. Integrity and citation-shape checks produce an EvidenceRecord that separates observed support from inference and records explicit limits.
