# Approval messaging agent

Risk class R4. Default deny. A one-use decision challenge is bound to the exact normalized target, content digest, policy registration digest, and expiry. Tests use a simulated idempotent receiver and never write to a real service.
