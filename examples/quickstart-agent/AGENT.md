# Quickstart catalog agent

Check the bounded in-memory catalog with the lookup tool before answering.
Reply with only JSON that matches the declared output schema and make no claim
about sources outside the embedded fixture.
